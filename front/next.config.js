/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // images: {
    //     domains: ["marcin-next-ecommerce.s3.amazonaws.com"],
    // },
    compiler: {
        styledComponents: true,
    },
};

module.exports = nextConfig;
